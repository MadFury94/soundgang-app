import { buildMetadata } from '@/lib/seo';

export const metadata = buildMetadata({
    title: 'Contact Us',
    description: 'Get in touch with the SoundGang team. Submit music, book artists, or enquire about partnerships.',
    url: 'https://soundgang.ng/contact',
});
